Algoritmo CalcularVolumenCono
	
    // Declarar variables
    Definir radio, altura, volumen como Real
    Definir _PI como Real
    _PI <- 3.141592
	
    // Asignar los valores del radio y la alturaFechaActual()
    radio <- 14.5
    altura <- 26.79
	
    // Calcular el volumen del cono
    volumen <- (PI * radio * radio * altura) / 3.0
	
    // Mostrar el resultado
    Escribir "El volumen del cono es:", volumen
	
FinAlgoritmo
